{
  "properties": [
    {
      "name": "jwt.secret.key",
      "type": "java.lang.String",
      "description": "Secret key used for signing JWT tokens. Must be at least 256 bits (32 characters) for HS256 algorithm. Should be different in each environment for security.",
      "sourceType": "com.bancom.app.demo.security.JwtTokenConfig"
    },
    {
      "name": "jwt.expiration.time",
      "type": "java.lang.Long",
      "description": "JWT token expiration time in milliseconds. Default is 3600000 (1 hour).",
      "sourceType": "com.bancom.app.demo.security.JwtTokenConfig",
      "defaultValue": 3600000
    }
  ]
}
